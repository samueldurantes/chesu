{
  "name": "chesu",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "client:dev": "pnpm --filter @chesu/client run dev",
    "server:dev": "cargo run",
    "schema": "sh scripts/generate_schema.sh"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
